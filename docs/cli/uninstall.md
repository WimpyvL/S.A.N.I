---
summary: "CLI reference for `sani uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `sani uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
sani uninstall
sani uninstall --all --yes
sani uninstall --dry-run
```
