# =============================================================================
# SANI Environment Variable Reference
# =============================================================================
# Copy this file to .env and fill in your values.
# All SANI_ variables are optional unless noted; sensible defaults apply.

# -----------------------------------------------------------------------------
# Core paths
# -----------------------------------------------------------------------------
# Override the state directory (default: ~/.sani)
# SANI_STATE_DIR=~/.sani

# Override the config file path (default: $SANI_STATE_DIR/sani.json)
# SANI_CONFIG_PATH=~/.sani/sani.json

# Override the OAuth credentials directory (default: $SANI_STATE_DIR/credentials)
# SANI_OAUTH_DIR=~/.sani/credentials

# -----------------------------------------------------------------------------
# Gateway
# -----------------------------------------------------------------------------
# Gateway listening port (default: 18789)
# SANI_GATEWAY_PORT=18789

# Gateway bind mode: loopback | lan | auto | custom (default: loopback)
# SANI_GATEWAY_BIND=loopback

# Gateway auth token (used when gateway.auth.mode = token)
# SANI_GATEWAY_TOKEN=

# Gateway auth password (used when gateway.auth.mode = password)
# SANI_GATEWAY_PASSWORD=

# -----------------------------------------------------------------------------
# Runtime flags
# -----------------------------------------------------------------------------
# Set to "1" when running under Nix (disables auto-install, enforces read-only config)
# SANI_NIX_MODE=0

# mDNS/Bonjour hostname advertised on the local network (default: sani)
# SANI_MDNS_HOSTNAME=sani

# Config cache lifetime in milliseconds (set to "0" to disable caching)
# SANI_CONFIG_CACHE_MS=

# Set to "0" to redact token previews in `sani status` output
# SANI_SHOW_SECRETS=1

# -----------------------------------------------------------------------------
# Twilio (WhatsApp channel)
# -----------------------------------------------------------------------------
# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=your_auth_token_here
# Must be a WhatsApp-enabled Twilio number, prefixed with whatsapp:
# TWILIO_WHATSAPP_FROM=whatsapp:+17343367101
